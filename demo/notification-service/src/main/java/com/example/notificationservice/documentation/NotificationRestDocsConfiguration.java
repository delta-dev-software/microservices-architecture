package com.example.notificationservice.documentation;
/*


import org.springframework.boot.test.context.TestConfiguration;
import org.springframework.context.annotation.Bean;
import org.springframework.restdocs.webtestclient.WebTestClientRestDocumentationConfigurer;
import org.springframework.test.web.reactive.server.WebTestClient;

@TestConfiguration
public class NotificationRestDocsConfiguration {

    @Bean
    public WebTestClient webTestClient(RestDocumentationContextProvider restDocumentation) {
        return WebTestClient.bindToServer()
                .filter(new WebTestClientRestDocumentationConfigurer(restDocumentation))
                .build();
    }
}
*/

